export interface ThemeInput {
  client?: string;
  clientDisplayName: string;
  customText: string;
  backgroundColor: string;
  primaryColor: string;
  textColor: string;
  contrastTextColor: string;
}
